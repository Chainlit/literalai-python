{
  "name": "chainlit-clients",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "main": "lint-staged.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "mypy": "mypy python-client",
    "formatPython": "black `git ls-files | grep '.py$'` && isort --profile=black ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^15.1.0"
  }
}
